# Portfolio 50-Persona Audit — Round 2 Progress (2026-09-08)

Protocol: `docs/portfolio-audit/2026-09-06-50-persona-audit.md`

This tracker records only evidence inspected in this continuation. Persona testing is simulated; runtime claims require actual CI/deployment/execution evidence.

| Repository | Round-2 result | New/regressed P0/P1/P2 | Evidence / next gate |
|---|---|---|---|
| `autodev-ng` | **NOT CLEAN** | **New P2 #6**; existing P2 #4 still present | Round-1 credential P0 #3 has landed remediation. New #6: unresolved configured env/file secret references become empty rather than failing during config/preflight. Existing #4 `--help` still exits as an error on current default source; PR #5 is open/unmerged. Recent product-code CI at `f925ba280c62d040ff783e82dd7569b1708b500b` passed typecheck + two full Vitest rounds; no provider/daemon/notification/Web/multi-worktree runtime pass is claimed. Repo report commit `7179d011862ad95b1565936754e6cb45a6670ff9`. |
| `taichung-police-intel` | **NOT CLEAN; #9 runtime acceptance now met** | No new P0/P1/P2 in #9 remediation path | PR #10 merged at `1273aff0f53ff09c87b74548fe7ee706a9f58e36`. Scheduled run `34069081364` completed refresh → V1/V2 verification → full gate/build → artifact → Pages deploy and produced a `MORNING` `SUCCEEDED` publication. Scheduled run `34140802075` completed the same path and current publication records `EVENING` `SUCCEEDED`. No regression of #9 observed. This is at most no-new-finding round 1/2; open P2 #12 and production-governance #5 still prevent CLEAN. Repo report commit `35674eaab4c071ebc8aea6fe17248db642f3099b`. |
| `flux-image-gen` | **NOT CLEAN** | **P1 #11 reopened — release-path regression / incomplete remediation** | Current `main` product SHA `ce69e934e43df5a9cc0d76337414c63d82b778de` contains fail-closed rate-limit logic and passed offline CI run `34066902969`, but `.github/workflows/deploy.yml` deploys production via raw `wrangler-action ... command: deploy`, bypassing `cloudflare/scripts/deploy.mjs` and `check-deploy-readiness.mjs`. Tracked `wrangler.toml` remains `ENVIRONMENT="development"` + `TURNSTILE_REQUIRED="false"`, so limiter absence/error is pass-through if that configuration is published. `production` remains 12 commits behind at `4d5c89482ba72a88189959988f7272007f801c67`; no post-fix production deploy/runtime evidence exists. Repo report commit `20a9c19edd884ce22c90ea2cff71bc88c19b3577`. |
| `voice-actress` | **NOT CLEAN** | **P1 #1 reopened — provenance regression / incomplete remediation** | Current `master` product SHA `de674011cac49d74693685381b4fae4fd9fe9826` adds fail-closed/opt-in fallback and provenance propagation, but `getProvenanceBadge(undefined)` renders the live `AI 實時評分` badge, while session normalization and legacy sanitization manufacture `{mode:"live"}` when provenance is absent. Unknown/legacy grading can therefore become authoritative-looking live AI provenance and contaminate live-only metrics. The Actions API reports zero workflow runs for this remediation SHA, so no build/test/deployment runtime pass is claimed. Repo report commit `0dae9cb7f3fa8e0f30a38512fa6c80e1c9da8873`. |
| `adng-memory` | **RUNTIME-PENDING — NOT CLEAN** | No new P0/P1/P2 in first post-remediation static round | Current `main` contract SHA `065c977da7c7ceb8c23a5c4110c3af326f084770` materially resolves Round-1 P2 #1: root README defines artifact ownership, retention, sensitive-data/recovery boundaries, and `scripts/health-check.ps1` provides a read-only heartbeat/snapshot/partial-write check. No distinct new static P0/P1/P2 passed for H05/C05/D03/D05/E03/I02/J03/J04. Actions reports zero runs for this SHA, so health/recovery execution and external-writer enforcement remain unverified. This is only no-new-finding round 1/2. Repo report commit `ba2a41414b6dca1276a42307f2ff52edc8e21769`. |
| `note-filler` | **NOT CLEAN** | **New P1 #4**; existing P2 #1 remains open | Current web flow stores the latest generated `CorrectionDoc` in process-global `app.state.last_doc`; `/export` reads that slot without a session/result ID or ownership check. Two clients/tabs sharing a process can therefore receive the other request's latest result, and concurrent `/run` requests are last-writer-wins. Classified P1 because this can produce a materially wrong legal/administrative export and a privacy disclosure on a shared process; no shared/public deployment is claimed. Current default SHA `04b310947e0dde32904df2b32e69a304d43c0494` has Actions run `33989418673` conclusion `failure`, so no runtime pass is claimed. Repo report commit `b6aee7f029f434f11a6c2653c4e1405d56822182`. |
| `minideck` | **NOT CLEAN** | No distinct new P0/P1/P2 after dedup; existing publication-boundary #4 remains open | Historical-version isolation #2 is present on current `main` SHA `d3026875d9ef0f0010137639e789359463139a89`, and CI run `34094862999` passed `npm run check`. The post-fix fixed-persona rerun reproduced the draft/public coupling (`current_version` is also the anonymous `/p/:id` head), but this exact fingerprint is already tracked by open #4. Temporary duplicate #5 was immediately closed as duplicate, and #4 was updated with persona/runtime acceptance linkage. No deployed Worker/browser publish/unpublish execution is claimed. Repo report commit `3fb825509e4a95d3acb694f26e7d90f5e62d678a`. |

## CLEAN accounting

No repository is newly marked CLEAN in this continuation. `autodev-ng` resets its no-new-finding streak because #6 is new. `taichung-police-intel` now has actual post-fix scheduled MORNING/EVENING deploy evidence for #9, but still does not meet the full portfolio stop criteria. `flux-image-gen` resets its no-new-finding streak because P1 #11 is reopened after the production GitHub Actions path was found to bypass the hardened deployment gate. `voice-actress` resets its no-new-finding streak because missing/legacy grading provenance is still promoted to live AI status, reopening P1 #1. `adng-memory` has one post-remediation no-new-finding static round but remains runtime-pending and therefore cannot be CLEAN. `note-filler` resets its no-new-finding streak because Round 2 adds P1 #4; existing P2 #1 also remains unresolved and current CI is red. `minideck` remains NOT CLEAN because existing #4 still blocks an explicit draft/published boundary and required runtime acceptance; this rerun did not add a distinct finding.