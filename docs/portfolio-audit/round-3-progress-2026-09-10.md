# Portfolio 50-Persona Audit — Round 3 Progress (2026-09-10)

Protocol: `docs/portfolio-audit/2026-09-06-50-persona-audit.md`

This continuation uses the same fixed 50 simulated personas and documented severity/stop criteria. Runtime claims require actual execution, CI or deployment evidence; repository/static evidence is not represented as live production validation.

| Repository | Round-3 result | New/regressed P0/P1/P2 | Evidence / next gate |
|---|---|---|---|
| `UkePack` | **NOT CLEAN** | **New P1 #5 — legacy authorization migration can orphan existing projects** | Current default snapshot `20e6ec5d09379afdcfff496a610e3b37f97c360e` contains the #1 capability-token remediation, but `migrate_project_schema()` silently assigns a fresh random `ukp_legacy_...` token to every pre-auth project row while the new authorization boundary requires that exact token for future access. A pre-upgrade browser/user cannot already possess the newly generated capability, and current source has no authenticated owner mapping, one-time claim flow, or operator handoff/recovery path. The migration test verifies only that a token exists, not that the legitimate prior owner can resume. Issue #5 tracks a secure custody/upgrade path. Existing P1 #2 remains open, and current-SHA Actions run `34158518182` is `failure`, so no green CI/runtime claim is made. Repo Round-3 report commit `56bcdb78da1e27529d70a9de71aa3414ddfcfb36`. |
| `cyber-prep-coach` | **NOT CLEAN** | **P1 #6 confirmed in fixed-persona Round 3 — explanation approval has no independent calibration** | Current default `5dd06b04ec49e7fa91855bd0957b482b3a1a0daf` is four audit/documentation commits on top of the last candidate-facing code `0f5dcca3902735b68b96611b97f70128544f51`. Current source uses one shared `gpt-5.3-codex-spark` model identifier for explanation generation and second-stage verification; dataset `1635bc64296d7ac1` reports 880/880 explanations approved and `explanationPending: 0`, while no independent SME/gold-set calibration or measured false-approval rate exists on default branch. This does not prove a current explanation is wrong; it makes independent correctness of the approval gate unmeasured and keeps #6 release-blocking. Current-SHA CI run `34400262454` is `failure`; its only job recorded zero steps, so no lint/build failure cause or current-SHA runtime pass is inferred. Round-3 linkage was added to existing #6 rather than opening a duplicate. Repo report commit `ab298d6070beff56e73061dd00756406fbbbef24`. |
| `chatgpt-dual-pipeline` | **NOT CLEAN** | **P1 #3 confirmed in fixed-persona Round 3 — non-published notes can enter the generated public site** | Current default `2299ea98063436b5d7995299d8baa166b14137d8` still has a sync script that copies included Markdown/JSON/CSV files without reading frontmatter release status. Three source notes (`2026-08-12`, `2026-08-13`, `2026-W33`) remain `status: scheduled`, corresponding generated files exist in `site/notes/`, and the site index links scheduled daily pages as ordinary content. No remediation PR matching the status-gate fingerprint was found. Issue #3 previously recorded production reachability on 2026-09-08; this continuation did not obtain a fresh production HTTP/browser receipt and does not extend that runtime claim. GitHub Actions returns zero workflow runs, so no current-SHA CI/runtime gate is claimed. Round-3 linkage was added to existing #3; repo report commit `1ade604e9ed0fe2b6ffa516b27cbf075f5e21eb8`. |
| `taiwan-intel-dashboard` | **Repo Round 2 — NOT CLEAN** | **P1 #17 confirmed — declared PAUSED state contradicts active scheduled deployment control plane** | Current default README still says paused / canonical 503 / automatic workflow disabled, while `.github/workflows/update-and-deploy.yml` still schedules fetch → state-save → audit → Cloudflare Pages deploy → deployed smoke. Actual run `34293170321` completed all of those jobs successfully, so the conflict is backed by execution evidence rather than prose inference. No fresh independent browser claim is made. Issue #17 was linked to the fixed personas and repo report commit `e60ffa079b35cc6d01f4a672242736b5b54c5716`; streak 0/2. |
| `exam-archive` | **Repo Round 2 — NOT CLEAN** | **P2 #3 confirmed — practice answers are pointer-only for keyboard/AT users** | Current product implementation remains unchanged under a documentation-only head: practice uses non-focusable `.mc-option` controls wired through `onclick`, without native radio/equivalent grouping and selected-state semantics. G01/G02 and related input-constrained personas cannot complete the practice answer path without a pointer. Prior Pages deployment proves artifact publication only, not keyboard/AT usability. Issue #3 now links the fixed-persona rerun; repo report commit `5d74726eed8f507bb9527aff2047945c6de10d79`. Existing #1 also remains open; streak 0/2. |
| `police-exam-archive` | **Repo Round 2 — NOT CLEAN** | **P2 #61 confirmed — public quality denominator is stale relative to the current corpus** | Current `master` manifest/tests use 36,760 non-duplicate choice questions while README quality claims and quiz copy retain 36,210; the 550 delta matches the documented 115-year unique-choice import. This does not prove those questions are invalid; it makes the scope of the public 100% quality claim ambiguous. #58 also remains open because PR #59 is unmerged. Protected-branch rules correctly rejected direct report creation, so the Round-2 report is preserved in PR #63 at commit `d6c65db207e63b79a64c0f7c9d08c19b922ce5c9`; no rule was bypassed. Streak 0/2. |
| `lobsterpulse` | **NOT CLEAN** | **P2 #5 and #6 incorporated; Round-2 P1 #3 is statically/CI-remediated but runtime-pending** | Current source semantically handles existing `codex_hooks=false`; merged Build run `34114733520` actually succeeded on Linux/macOS/Windows with hook tests and release build, so the old static #3 failure no longer reproduces. #3 stays open because packaged real-Codex runtime evidence is still absent. New fixed-persona blockers are #5 (13-provider/K0 denominator truth contract) and #6 (desktop product has no GitHub Release while README points at a local build artifact). GitHub Releases currently returns an empty list. Repo Round-3 report commit `5c09da0cf7b01fcfd80c60ee2a529ae650aed265`; streak 0/2. |

## CLEAN accounting

No repository is newly marked CLEAN in this continuation. `UkePack` resets its consecutive no-new-finding streak because Round 3 adds P1 #5. The original #1 cross-project IDOR is materially improved in current source and is not reopened here, but the upgrade custody gap, existing P1 #2, red current-SHA CI, and missing two-user/browser/legacy-upgrade runtime evidence keep the repository **NOT CLEAN**.

`cyber-prep-coach` also resets its consecutive no-new-finding streak because fixed-persona Round 3 incorporates P1 #6 as a reproducible release-trust gap. The finding is bounded: same-model generation/verification plus zero independent calibration does not establish factual errors in the 880 explanations; it establishes that the current all-approved state is not independently calibrated. #6 remains open, current-SHA CI is not green, and the previously documented real-browser/accessibility/recovery/release evidence remains incomplete, so the repository is **NOT CLEAN**.

`chatgpt-dual-pipeline` resets its streak because P1 #3 remains reproducible on current source. The defect is the publication gate itself, not a claim that sensitive data is currently exposed: `scheduled` state is not enforced during sync/build preparation and appears in generated site content. Until one fail-closed eligibility contract governs all publication surfaces and excluded slugs are runtime-verified absent after remediation, the repository remains **NOT CLEAN**.

`taiwan-intel-dashboard` is **NOT CLEAN** and resets to 0/2 because current default-branch status prose and scheduled production behavior still conflict under P1 #17. The execution evidence is the GitHub Actions fetch/audit/deploy/smoke chain; browser-visible current state remains separately unverified.

`exam-archive` is **NOT CLEAN** and resets to 0/2 because Round 2 incorporates P2 #3. The static barrier is sufficient to confirm pointer-only practice controls; closure still requires actual keyboard and screen-reader/browser acceptance after remediation, and existing #1 remains unresolved.

`police-exam-archive` is **NOT CLEAN** and resets to 0/2 because Round 2 incorporates P2 #61 while #58 is still unresolved on default branch. Candidate evidence in PR #59 is not treated as merged/default/production evidence. The audit report itself is in PR #63 because master protection refused a direct write.

`lobsterpulse` is **NOT CLEAN**. The Round-2 #3 source/configuration regression now passes current-source inspection and merged multi-platform CI, but the required packaged Codex runtime acceptance is still missing. Round 3 also incorporates P2 #5 and #6, so the no-new-finding streak resets to 0/2; provider-support truth and a real distribution/source-only contract must be resolved before CLEAN can start.
