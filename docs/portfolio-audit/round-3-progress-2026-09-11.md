# Portfolio 50-Persona Audit — Round 3 Progress (2026-09-11)

Protocol: `docs/portfolio-audit/2026-09-06-50-persona-audit.md`

This continuation uses the same fixed 50 simulated personas and documented severity/stop criteria. Runtime claims require actual execution, CI or deployment evidence; repository/static evidence is not represented as live production validation.

| Repository | Round-3 result | New/regressed P0/P1/P2 | Evidence / next gate |
|---|---|---|---|
| `cf-ai-router` | **NOT CLEAN** | **New P2 #5 — metered Workers AI cost state fails open across missing config / billing-plan transition** | Default branch before the repo audit report was `049ec8772b1fe2ed54491f056a36cce014bf6ede`. `isMetered()` classifies Workers AI as metered, while `ManualRouter.workersAiEnabled` uses `WORKERS_AI_ENABLED !== "false"`, so a missing variable enables the provider. Checked-in `wrangler.toml` also sets the flag `true` in production/dev based on the current Free-plan assumption. Cloudflare's current Workers AI pricing (checked 2026-09-11) says Workers Paid is charged above the 10,000-Neuron/day free allocation, so an external Free→Paid account transition can change cost semantics without changing repository config. No claim is made that the current account is Paid or that charges occurred. Current-main CI run `34099221520` is a real success and ran `npm ci` + `npm run check` (typecheck + Vitest + bundle), but that does not establish billing-plan state or live cost/fallback behavior. Issue #5 requires missing state to fail closed and Paid-metered use to require explicit opt-in or verified deployment guard. Repo Round-3 report commit `74c52130046a9f3e654fae3fe42f9bbc2224baeb`; streak resets to 0/2. |
| `voice-actress` | **NOT CLEAN** | **P2 #7 newly incorporated — README/definitive architecture still advertise removed Stripe billing and no-DB storage; P1 #1 remains reproducible** | Current product/default snapshot before the repo report was `1f4657b44c942062f23909b9f59e95e5396707be`. `README.md` still advertises Stripe env vars, billing routes/page, paid pro/team pricing and all-flat-file/no-database persistence, while `package.json` has `better-sqlite3` and no runtime `stripe`, `lib/shenlun-sessions-store.ts` creates/writes a SQLite `sessions` table, and the documented billing status route is absent. The current definitive architecture map still models Stripe + `Flat-file Stores (NO DB)`. Existing P1 #1 also still reproduces: missing provenance is rendered as `AI 實時評分` and `normalizeSessionInput()` manufactures `mode:"live"` when provenance is omitted. GitHub Actions reports zero workflow runs for `master`, so no current-SHA install/test/build/browser/provider/restore runtime claim is made. Existing Issue #7 was updated rather than duplicated. Repo Round-3 report commit `120523b73872155457bf2251cb02b66c950ad4c2`; streak resets to 0/2. |
| `taiwan-intel-dashboard` | **NOT CLEAN** | **P1 #18 newly incorporated — empty/truncated AI narrative can become literal `暫無資料` while non-empty evidence exists and `summary.ok=true`; P1 #17 remains open** | Current product/default snapshot before the repo report was `e60ffa079b35cc6d01f4a672242736b5b54c5716`. `summarize()` still substitutes `（暫無資料）` for empty domestic/international narrative text; `fetch-live.mjs` then marks `status.summary={ok:true}` whenever summarize returns. `pipeline-audit.yml` has source/network/build/provenance/size gates but no summary-content invariant. Issue #18 already preserves actual historical execution evidence from run `34293170321`, where a non-empty candidate was deployed with both headline briefs saying no data. Round 3 does not claim a fresh recurrence of that exact production artifact. At inspection time scheduled current-head run `34544474067` was `in_progress`; that proves only that the workflow is active, not success or summary integrity. Existing #18 was updated rather than duplicated. Repo Round-3 report commit `3367e15eb69e03f9e6d38c94631f287a6f75e088`; streak remains/resets to 0/2. |
| `adng-memory` | **NOT CLEAN** | No distinct new P0/P1/P2; existing P2 #4 lifecycle-governance gap remains | Current contract still defines ownership, retention, sensitive-data boundary and safe recovery, and the original P2 #1 does not reappear. `scripts/health-check.ps1` remains read-only. However #4 still tracks the missing machine-verifiable activation/supersession/tombstone contract and the README explicitly states writer enforcement is external. GitHub Actions reports zero runs, so the health check, external writer, stale-overwrite and tombstone/recovery paths are not claimed as executed. Repo Round-3 report commit `2a2cc88244cd9f7b754a984f817126d11111f161`. This is another no-new static pass, but not a qualifying CLEAN round while #4/runtime evidence remain unresolved. |
| `taichung-police-intel` | **NOT CLEAN** | No new P0/P1/P2 in the #9 scheduled-publication path; existing P2/governance #5 and #12 remain | Scheduled run `34546270369` is real execution evidence: refresh, V1 verification, persistent V2 build/verification, full gate/static build, evidence commit, Pages artifact upload and Pages deploy all completed successfully. The previous #9 scheduled-publication regression did not reappear in that run. Repository rulesets currently return `[]`; branch-protection state cannot be substituted because that endpoint is inaccessible to the integration. #5 therefore remains unverified/open, and #12 remains an open P2 product contract. Repo Round-3 report commit `e1d081bd04824c062c7ee99e7d74f9e478240743`. |
| `flux-image-gen` | **NOT CLEAN** | Existing P1 #11 remains reproducible; no distinct new default-branch finding | Current deploy workflow still sends production directly through `wrangler-action ... command: deploy` without the hardened readiness script, while checked-in `wrangler.toml` remains `ENVIRONMENT="development"` and `TURNSTILE_REQUIRED="false"`. `production` remains at `4d5c89482ba72a88189959988f7272007f801c67`, before the remediation. Open PR #20 adds candidate tests but is unmerged and does not alter the production deploy fingerprint; candidate CI is not treated as default-branch/production validation. Repo Round-3 report commit `dfadcf30ca1d3daf479e05dd97aa457afa265333`. |
| `note-filler` | **NOT CLEAN** | Existing P1 #4 remains reproducible; no distinct new P0/P1/P2 | Current `app/server.py` still stores the last generated `CorrectionDoc` in one process-global `app.state.last_doc`; `/run` overwrites it and `/export` reads it without result/session/owner binding. The same two-client/last-writer-wins correctness and privacy fingerprint remains. Latest default-branch Actions run `34190108953` is `failure`; no shared/public deployment or live data disclosure is claimed. Repo Round-3 report commit `9b579adb0391f9a96f620f2d58d4a7f0e420c4df`. |
| `project-doctor-web` | **NOT CLEAN** | Existing P1 #2 and #9 remain unresolved; no distinct new finding | No product-code remediation landed after Round 2. #2 still lacks proven shared/atomic global cost protection and fail-safe durable-storage behavior; #9 still has the prompt/data-contract path that can represent unobserved normal physical-exam findings as Objective facts. Historical PR Actions are not current cross-isolate/provider/browser/clinical validation. Repo Round-3 report commit `bb2cc69dc202af5575d11eb958bda56a01f7de11`. |

## CLEAN accounting

`cf-ai-router` remains **NOT CLEAN**. Round 1 and Round 2 produced no new static P0/P1/P2 findings, but Round 3 adds P2 #5, so its consecutive no-new-finding counter resets to **0/2**. Existing current-main CI is valid execution evidence for the repository test/build gate only; it is not treated as proof of current Cloudflare account plan, provider quota state, production deployment revision, or real fallback/cost behavior. The repository cannot be marked CLEAN until #5 is resolved/dispositioned, the same fixed personas are rerun after the fix, required current/recent runtime paths have evidence, and two consecutive rounds produce no new P0/P1/P2 findings.

`voice-actress` remains **NOT CLEAN**. Round 3 newly incorporates P2 #7 into the fixed-persona sequence, so its consecutive no-new-finding counter is **0/2**. The finding is bounded to a truthful product/deployment/storage contract: current evidence does not show an unexpected charge, data loss or production outage. Separately, Round-2 P1 #1 remains reproducible on current source because missing/legacy provenance can still be upgraded to a live-AI claim. With zero GitHub Actions runs on current `master`, neither static remediation claims nor historical commit-message validation are treated as current runtime evidence. CLEAN cannot begin until #1/#7 and any other P0/P1/P2 blockers are resolved/dispositioned, required current/recent runtime paths are evidenced, and the same 50 personas produce no new P0/P1/P2; final CLEAN still requires a second consecutive qualifying round.

`taiwan-intel-dashboard` remains **NOT CLEAN**. Round-2 P1 #17 is still unresolved, and Round 3 explicitly incorporates P1 #18 into the fixed-persona sequence, so the consecutive no-new-finding counter is **0/2**. Historical Actions run `34293170321` is valid execution/deployment evidence for the recorded summary-integrity failure, while current run `34544474067` was still in progress when inspected and is not treated as a pass. The repository cannot begin a CLEAN streak until #17/#18 and any other P0/P1/P2 blockers are resolved/dispositioned, required paused/restore/active and summary-degradation runtime receipts are retained on current/recent code, and the same 50 personas produce no new P0/P1/P2; final CLEAN still requires a second consecutive qualifying round.

`adng-memory` remains **NOT CLEAN**. The original #1 documentation/recovery gap is still materially remediated and Round 3 adds no distinct new P0/P1/P2, but open P2 #4 plus absent external-writer/runtime evidence mean the static no-new sequence cannot be promoted to the portfolio CLEAN condition.

`taichung-police-intel` remains **NOT CLEAN**. Current scheduled execution provides strong positive runtime evidence for the #9 publication path, but open #5/#12 still prevent a qualifying CLEAN streak. The audit does not infer branch protection from an endpoint it cannot access, and it does not turn an empty ruleset list into proof about every possible legacy protection mechanism.

`flux-image-gen` remains **NOT CLEAN** because P1 #11 is unchanged on default/release evidence. An unmerged test-only candidate does not satisfy the production deployment-path gate, and no post-fix production Worker runtime receipt exists.

`note-filler` remains **NOT CLEAN** because P1 #4 is unchanged and current-default CI is red. A source-level two-client isolation defect is sufficient to keep the core export path blocked even though this audit does not claim the service is publicly deployed.

`project-doctor-web` remains **NOT CLEAN** because P1 #2 and #9 remain unresolved. No current/default execution evidence was found that would satisfy the durable cross-isolate abuse-control or provenance-safe clinical Objective acceptance paths.

This file continues the cumulative Round-3 tracker in `round-3-progress-2026-09-10.md`; prior repository statuses remain unchanged unless a later continuation explicitly updates them.
