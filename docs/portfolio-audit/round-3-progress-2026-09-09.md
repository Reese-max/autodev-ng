# Portfolio 50-Persona Audit — Round 3 Progress (2026-09-09)

Protocol: `docs/portfolio-audit/2026-09-06-50-persona-audit.md`

This tracker continues the same fixed 50 simulated personas and documented severity/stop criteria. Runtime claims require actual execution, CI or deployment evidence; repository/static evidence is not represented as live production validation.

| Repository | Round-3 result | New/regressed P0/P1/P2 | Evidence / next gate |
|---|---|---|---|
| `92-duty-scheduler` | **NOT CLEAN** | **P0 #14 reopened — post-remediation authorization regression** | Current product SHA `9a916b5ada2b1994d14bccdd780eb70ac564334d` adds HMAC-bound student tokens and fail-closed admin auth, but anonymous `GET /api/students/timetable?id=<sid>` now mints/returns a valid student write token for whichever existing ID the caller supplies. The same anonymous caller can POST that token for the victim and pass `verifyStudentToken()`. Existing tests sign tokens directly and do not exercise anonymous GET→POST credential bootstrap. CI run `34052371166` succeeded on the remediation SHA, proving the checked-in test gate passed but not the missed auth path. `production` remains at `ccddd897f160ad39edabea8c47ef86a65d692b0f`, predating the remediation; no deployed exploit/safety claim is made. Repo report commit `3c183d902045014b98ca5e396aef97663f39d9e6`. |
| `tick-stock-panel` | **NOT CLEAN** | **P2 #2 reopened — coverage remediation lacks workflow enforcement** | Fix SHA `565ccdfee6c2fb0c079cd4192edb9cb31c9157f4` adds the root trust contract, normalized provider coverage/status metadata, Fugle watchlist semantics and provider-aware UI, materially addressing #1 and part of #2. Fixed-persona rerun found that `Screener.tsx` only renders advisory scope/empty-universe messaging; auto-run/manual run paths are not gated by `coverage`, `partial` or `empty_universe`, and backend `run_custom`/`run_preset` likewise execute without a quote-coverage precondition. This misses #2's explicit fail-closed-or-acknowledge acceptance for watchlist/unknown/partial and the empty-watchlist blocked-run case. Actions run `34102027415` succeeded, but the job only built/pushed Docker and did not execute the added routing tests or browser/provider gating matrix. No live Fugle/deployment behavior is claimed. Repo report commit `36435398062cae2ae496a74228a98dab5d82cc8b`. |
| `clinical-scribe-worker` | **NOT CLEAN** | **New P0 #6 — post-remediation authentication bypass** | Current product SHA `88bb7469508e8732aaff6f21d048c8677823fc04` adds fail-closed auth/quota controls and reviewer authorization, but shared `verifyCfAccessJwt()` only decodes claims and checks time/issuer/audience; it never verifies the JWT signature. The positive Access test intentionally supplies `header.payload.mock-signature` and expects it to pass, while README claims signatures are verified. The same verifier protects general server-funded API access and maps Access email to reviewer `scope:*`, so forged claims can satisfy both trust boundaries at source level. Issue #6 tracks cryptographic JWKS/signature verification and regression tests. CI run `34068143837` succeeded, but its executed job only ran `npm ci` + `npm run typecheck`; it did not execute Vitest security tests. `production` remains at `a4c29a3abfc273a01973971885003e5e5d42086c`, predating the reviewed remediation; no deployed exploit/runtime claim is made. Repo report commit `b1f193b0bb5432ea4961e346d14c9c2cf5392181`. |
| `avatar-vfo` | **NOT CLEAN** | **P0 #2 reopened — Cloudflare Access remediation still accepts forged-signature JWTs** | Remediation SHA `b94ae9b82df76055301a25f4d9aa6e7a375d11b1` correctly removes the predictable production secret and rejects email-header-only identity, but `worker/src/auth.ts::verifyCfAccessHeaders()` only parses/checks JWT header/claims and never verifies `parts[2]` against Access JWKS/certs. The checked-in regression test constructs a token ending `.fakesig` and expects `/api/avatars` HTTP 200, directly missing #2's cryptographic-validation criterion. CI run `34067753862` genuinely passed Worker typecheck+Vitest, frontend test/build, YAML validation, and backend ruff/mypy/pytest; this proves the flawed expectation passed CI rather than proving auth safety. Current `main` is docs-on-top SHA `9e2dafa749e36746047a6b9947acabdcb0b4dd4c`; `production` remains old at `b6eb2f0e93377241a07fcc4460c97be8f0e5d7a6`. No deployed exploit/runtime claim is made. Repo report commit `1226629dba0e82921504beb9169e4cdf43ad3797`. |

## CLEAN accounting

No repository is newly marked CLEAN in this continuation. `92-duty-scheduler` resets its consecutive no-new-finding streak because P0 #14 is reopened. #15's missing-`ADMIN_TOKEN` helper behavior is positive current static evidence, but #12 remains open and required deployment/mobile/accessibility/runtime evidence is still incomplete. `tick-stock-panel` also resets its streak because P2 #2 is reopened after the post-fix rerun found that the normalized coverage contract is advisory rather than enforced at screening execution boundaries; required CI/browser/provider evidence remains incomplete. `clinical-scribe-worker` resets its streak because the post-remediation fixed-persona rerun found P0 #6: Cloudflare Access assertions are accepted without cryptographic signature verification. `avatar-vfo` also resets its streak because P0 #2 is reopened: email-header spoofing is fixed, but forged-signature Access JWTs are still accepted by current source and even encoded as a passing test. Both tracked production branches predate the reviewed remediation code, so no deployed exploit or current production behavior is claimed.
