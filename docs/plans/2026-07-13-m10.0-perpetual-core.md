# M10.0 外環＋台帳（永續工程師骨架）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** daemon idle 時自主立案：discovery → 台帳去重 → value 閘 → 自寫 GOAL → 跑既有 autopilot session → 收案回寫台帳——無人給目標也持續工作。

**Architecture:** daemon 內建外環（單進程防雙工）。kernel 只加薄接線（daemon idle 分支＋types config 欄位＋digest 一行）；主邏輯在 `src/autopilot/`（perpetual.ts / ledger.ts / session.ts 不吃 kernel 帳）。**循環 import 解法**：`finalizeRunOnceHeartbeat` 由 cli.ts 搬家至 scheduler.ts；session 核心抽到新檔 session.ts（不 import cli.js）。spec：`docs/specs/2026-07-13-m10-perpetual-engineer-design.md`。

**Tech Stack:** TypeScript、better-sqlite3、zod、vitest（serial）。

## Global Constraints

- **kernel 帳** `wc -l src/*.ts | tail -1` ≤2700（現 2549；本計畫 kernel 面預估 +40~55，收在 ~2600）。每 task 結束實測。
- `npm run build`＋`npx vitest run`（serial，611 tests 基線）全綠；禁改 vitest.config.ts；測試禁 sleep（可注入假 now/sleepFn）。
- 外環全鏈 fail-open：任何故障不得反殺 daemon（鐵律 #4）。`cfg.perpetual` 未設（false）時全部行為與 M9.9 byte-identical。
- 既有 autopilot 測試（run/orchestrator/evaluator/supplement/discover）**全綠且不改斷言**——run.ts 重構的回歸線。
- conventional commit 繁中，結尾兩行 trailer：
  ```
  Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01AApy1X84bUY26GBvyPskRp
  ```
- commit 前 `git diff --cached --name-only` 嚴格核對預期清單；分支 `m10.0-perpetual-core`。
- **不要 commit** `scripts/adng-bot.cmd`、`scripts/adng-daemon.cmd` 的既有未提交變更（PONYTAIL 防護，不屬本計畫）。

---

### Task 1: 問題台帳 ledger.ts（problems 表＋API）

**Files:**
- Create: `src/autopilot/ledger.ts`
- Test: `tests/autopilot-ledger.test.ts`

**Interfaces:**
- Consumes: better-sqlite3（既有依賴）、node:crypto。
- Produces（後續 task 依賴的精確簽名）:
  ```ts
  export interface ProblemCandidate { title: string; lens: string; value: number }
  export interface ProblemRow {
    fingerprint: string; title: string; lens: string; value: number
    status: 'open' | 'in-progress' | 'fixed' | 'deferred' | 'rejected'
    goalId: string; firstSeen: string; lastSeen: string; note: string
  }
  export function problemFingerprint(title: string): string
  export class ProblemsLedger {
    constructor(dbFile: string)                       // join(dataDir,'run.db')；自開連線，不動 kernel db.ts
    upsertSeen(c: ProblemCandidate, nowIso: string): { isNew: boolean; row: ProblemRow }
    setStatus(fingerprint: string, status: ProblemRow['status'], note: string, goalId?: string): void
    listByStatus(status: ProblemRow['status'], limit?: number): ProblemRow[]  // value DESC
    counts(): Record<string, number>                  // status → 件數
    close(): void
  }
  ```

- [ ] **Step 1: 寫失敗測試** `tests/autopilot-ledger.test.ts`

```ts
import { describe, test, expect, beforeEach, afterEach } from 'vitest'
import { mkdtempSync, rmSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { ProblemsLedger, problemFingerprint } from '../src/autopilot/ledger.js'

describe('ProblemsLedger', () => {
  let dir: string; let ledger: ProblemsLedger
  beforeEach(() => { dir = mkdtempSync(join(tmpdir(), 'adng-ledger-')); ledger = new ProblemsLedger(join(dir, 'run.db')) })
  afterEach(() => { ledger.close(); rmSync(dir, { recursive: true, force: true }) })

  test('fingerprint 正規化：NFKC＋小寫＋去空白標點——等價標題同指紋', () => {
    expect(problemFingerprint('API 缺 錯誤處理！')).toBe(problemFingerprint('api缺錯誤處理'))
    expect(problemFingerprint('ＡＰＩ缺錯誤處理')).toBe(problemFingerprint('api缺錯誤處理')) // 全形 NFKC
    expect(problemFingerprint('a')).not.toBe(problemFingerprint('b'))
  })

  test('upsertSeen：新問題 INSERT status=open；重複發現只更新 lastSeen/value', () => {
    const a = ledger.upsertSeen({ title: 'X 未測', lens: 'tests', value: 7 }, '2026-07-13T00:00:00Z')
    expect(a.isNew).toBe(true); expect(a.row.status).toBe('open'); expect(a.row.firstSeen).toBe('2026-07-13T00:00:00Z')
    const b = ledger.upsertSeen({ title: 'x未測！', lens: 'perf', value: 9 }, '2026-07-14T00:00:00Z') // 等價標題
    expect(b.isNew).toBe(false); expect(b.row.value).toBe(9)
    expect(b.row.firstSeen).toBe('2026-07-13T00:00:00Z'); expect(b.row.lastSeen).toBe('2026-07-14T00:00:00Z')
    expect(b.row.lens).toBe('tests') // lens 保留首見值（不入指紋、不覆寫）
  })

  test('setStatus/listByStatus/counts：狀態機與查詢', () => {
    ledger.upsertSeen({ title: 'p1', lens: 'tests', value: 5 }, '2026-07-13T00:00:00Z')
    ledger.upsertSeen({ title: 'p2', lens: 'correctness', value: 8 }, '2026-07-13T00:00:00Z')
    ledger.setStatus(problemFingerprint('p2'), 'fixed', 'goal 達成', 'ab12')
    const open = ledger.listByStatus('open')
    expect(open.map(r => r.title)).toEqual(['p1'])
    const fixed = ledger.listByStatus('fixed')
    expect(fixed[0]!.goalId).toBe('ab12'); expect(fixed[0]!.note).toBe('goal 達成')
    expect(ledger.counts()).toEqual({ open: 1, fixed: 1 })
  })

  test('冪等重開：同檔開兩次不炸，資料仍在', () => {
    ledger.upsertSeen({ title: 'p1', lens: 'tests', value: 5 }, '2026-07-13T00:00:00Z')
    ledger.close()
    ledger = new ProblemsLedger(join(dir, 'run.db'))
    expect(ledger.listByStatus('open')).toHaveLength(1)
  })
})
```

- [ ] **Step 2: RED** — `npx vitest run tests/autopilot-ledger.test.ts`，預期 FAIL（模組不存在）。

- [ ] **Step 3: 實作** `src/autopilot/ledger.ts`

```ts
import Database from 'better-sqlite3'
import { createHash } from 'node:crypto'

/** M10.0 問題台帳：discovery 結果的持久記憶（去重＋狀態機）。對 run.db 自開第二條連線
 * （WAL 併存安全；寫入者只有 daemon 進程內的外環——單寫者紀律），不動 kernel db.ts。
 * 台帳是記憶不是閘門：呼叫端一律 fail-open，寫入失敗降級為失憶，不得擋工作。 */

export interface ProblemCandidate { title: string; lens: string; value: number }
export interface ProblemRow {
  fingerprint: string; title: string; lens: string; value: number
  status: 'open' | 'in-progress' | 'fixed' | 'deferred' | 'rejected'
  goalId: string; firstSeen: string; lastSeen: string; note: string
}

/** 指紋＝NFKC 正規化→小寫→剝除空白與標點後 sha1 前 16 hex。lens 不入指紋——
 * 同一問題換鏡頭再現仍算同案（spec §3.3）。 */
export function problemFingerprint(title: string): string {
  const norm = title.normalize('NFKC').toLowerCase().replace(/[\s\p{P}\p{S}]/gu, '')
  return createHash('sha1').update(norm).digest('hex').slice(0, 16)
}

export class ProblemsLedger {
  private db: Database.Database
  constructor(dbFile: string) {
    this.db = new Database(dbFile)
    this.db.pragma('journal_mode = WAL')
    this.db.exec(`CREATE TABLE IF NOT EXISTS problems (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      fingerprint TEXT NOT NULL UNIQUE,
      title TEXT NOT NULL,
      lens TEXT NOT NULL DEFAULT '',
      value INTEGER NOT NULL DEFAULT 0,
      status TEXT NOT NULL DEFAULT 'open',
      goal_id TEXT NOT NULL DEFAULT '',
      first_seen TEXT NOT NULL,
      last_seen TEXT NOT NULL,
      note TEXT NOT NULL DEFAULT ''
    )`)
  }

  upsertSeen(c: ProblemCandidate, nowIso: string): { isNew: boolean; row: ProblemRow } {
    const fp = problemFingerprint(c.title)
    const existing = this.get(fp)
    if (existing) {
      this.db.prepare(`UPDATE problems SET last_seen=?, value=? WHERE fingerprint=?`).run(nowIso, c.value, fp)
      return { isNew: false, row: this.get(fp)! }
    }
    this.db.prepare(`INSERT INTO problems (fingerprint,title,lens,value,status,first_seen,last_seen)
      VALUES (?,?,?,?, 'open', ?, ?)`).run(fp, c.title, c.lens, c.value, nowIso, nowIso)
    return { isNew: true, row: this.get(fp)! }
  }

  setStatus(fingerprint: string, status: ProblemRow['status'], note: string, goalId = ''): void {
    this.db.prepare(`UPDATE problems SET status=?, note=?, goal_id=CASE WHEN ?='' THEN goal_id ELSE ? END WHERE fingerprint=?`)
      .run(status, note, goalId, goalId, fingerprint)
  }

  listByStatus(status: ProblemRow['status'], limit = 50): ProblemRow[] {
    return (this.db.prepare(`SELECT fingerprint,title,lens,value,status,goal_id,first_seen,last_seen,note
      FROM problems WHERE status=? ORDER BY value DESC, id ASC LIMIT ?`).all(status, limit) as Record<string, unknown>[]).map(toRow)
  }

  counts(): Record<string, number> {
    const rows = this.db.prepare(`SELECT status, COUNT(*) n FROM problems GROUP BY status`).all() as { status: string; n: number }[]
    return Object.fromEntries(rows.map(r => [r.status, r.n]))
  }

  private get(fp: string): ProblemRow | undefined {
    const r = this.db.prepare(`SELECT fingerprint,title,lens,value,status,goal_id,first_seen,last_seen,note
      FROM problems WHERE fingerprint=?`).get(fp) as Record<string, unknown> | undefined
    return r ? toRow(r) : undefined
  }

  close(): void { this.db.close() }
}

function toRow(r: Record<string, unknown>): ProblemRow {
  return {
    fingerprint: r.fingerprint as string, title: r.title as string, lens: r.lens as string,
    value: r.value as number, status: r.status as ProblemRow['status'], goalId: r.goal_id as string,
    firstSeen: r.first_seen as string, lastSeen: r.last_seen as string, note: r.note as string
  }
}
```

- [ ] **Step 4: GREEN＋kernel 帳** — 該測試檔 PASS；`npm run build` 過；`wc -l src/*.ts | tail -1` 記錄（本 task 應為 0 增量——全在子目錄）。

- [ ] **Step 5: Commit** — `git add src/autopilot/ledger.ts tests/autopilot-ledger.test.ts`，核對清單，`feat(autopilot): 問題台帳——problems 表+指紋去重+狀態機`。

---

### Task 2: session 核心抽取（解循環 import＋run.ts 薄殼化）

**Files:**
- Modify: `src/cli.ts`（`finalizeRunOnceHeartbeat` :267-275 整段搬走，原地改 re-export）
- Modify: `src/scheduler.ts`（收編 `finalizeRunOnceHeartbeat`）
- Create: `src/autopilot/session.ts`（session 核心，從 run.ts main() 抽出）
- Modify: `src/autopilot/run.ts`（main() 薄殼化）
- Test: 既有全套測試（**不新增、不改斷言**——純重構的回歸線）

**Interfaces:**
- Consumes: run.ts main() 現有全部內容（`src/autopilot/run.ts:21-104`）；cli.ts `finalizeRunOnceHeartbeat`（:267-275）。
- Produces:
  ```ts
  // scheduler.ts 新增（函式體逐字搬自 cli.ts:267-275，import 補 localDay from './db.js'——subscriptionTags 本來就在檔內）
  export function finalizeRunOnceHeartbeat(deps: Deps, result: CycleResult, now: Date = new Date()): void

  // cli.ts 原地保留相容出口（既有 import 不破）：
  export { finalizeRunOnceHeartbeat } from './scheduler.js'

  // session.ts（不 import cli.js——這是本 task 的存在理由）
  export interface SessionResult {
    goalId: string
    outcome: GoalOutcome                                  // from orchestrator.js
    supplement?: { clean: boolean; rounds: number; supplemented: number; residualGaps: string[] }
  }
  export function stopAlertMessage(goalId: string, outcome: GoalOutcome): string | null   // 搬自 run.ts
  export async function runGoalWithDeps(
    deps: Deps, notifier: { send(text: string): Promise<boolean> }, cfg: Config,
    opts?: { discovered?: DiscoverResult }
  ): Promise<SessionResult | 'no-goal' | 'lock-busy'>
  ```
- `runGoalWithDeps` 的函式體＝run.ts main() :23-103 逐字搬移，僅四處改動：
  1. 開頭 `assemble(cfgPath)` 移除（deps/notifier/cfg 改由參數收）。
  2. discovery 段包上 `if (opts?.discovered) { discovered = opts.discovered } else if (cfg.surveyCommand) { ...既有邏輯... }`——外部傳入時不重跑。
  3. 各階段結果收進 `SessionResult` 回傳（原本只 console.log；console.log 保留不動）。
  4. `no GOAL.md` 與 lock busy 兩個早退路徑改回傳字串常量（原本 return void）。
- run.ts main() 薄殼＝`const { deps, notifier, cfg } = assemble(cfgPath); await runGoalWithDeps(deps, notifier, cfg)`；`export { stopAlertMessage } from './session.js'` 保住既有測試 import。

- [ ] **Step 1: 基線快照** — `npx vitest run > /tmp/before.txt; echo EXIT=$?`（直跑收 exit code，禁止管線吞碼），記錄通過數（基線 611）。
- [ ] **Step 2: 搬 finalizeRunOnceHeartbeat** — scheduler.ts 收編（含 doc 註解逐字）、cli.ts 段落刪除＋改 re-export、run.ts import 改自 `'../scheduler.js'`。`npm run build` 過。
- [ ] **Step 3: 抽 session.ts** — 按上述四處改動抽取；run.ts 薄殼化。`npm run build` 過。
- [ ] **Step 4: 回歸驗證** — `npx vitest run; echo EXIT=$?` 全綠且通過數＝基線（斷言零修改）。`wc -l src/*.ts | tail -1`（cli.ts 淨 -8、scheduler.ts 淨 +9 左右，記錄實測）。
- [ ] **Step 5: Commit** — `git add src/cli.ts src/scheduler.ts src/autopilot/session.ts src/autopilot/run.ts`，`refactor(autopilot): session 核心抽至 session.ts+heartbeat 搬 scheduler——斷 daemon→cli 循環 import`。

---

### Task 3: 自寫 GOAL（authorGoal）＋外環狀態檔

**Files:**
- Create: `src/autopilot/author.ts`
- Test: `tests/autopilot-author.test.ts`

**Interfaces:**
- Consumes: `parseGoal`（`src/autopilot/goal.js`）；LLM 呼叫沿用 `src/autopilot/llm.ts` 既有匯出（實作前先讀該檔與 planner.ts 的呼叫慣例，簽名以檔內為準——本計畫以 `chat(llm, prompt): Promise<string>` 代稱）。
- Produces:
  ```ts
  export interface RankedProblem { value: number; title: string; lens: string; rationale: string }  // 對齊 discover.ts parseRanked 輸出型別（實作時 import 該型別，勿重複宣告）
  export const AUTO_GOAL_MARKER = '<!-- adng:auto-goal'   // 完整行：`<!-- adng:auto-goal problem:<fp> -->`
  export function isAutoGoal(md: string): boolean          // 首個非空行含 AUTO_GOAL_MARKER
  export async function authorGoal(
    llm: { url?: string; model: string; apiKey: string },
    problem: RankedProblem, cfg: Config, fingerprint: string
  ): Promise<string | null>   // 回傳完整 GOAL.md 內容；LLM 產出無法成案回 null

  export interface PerpetualState { lastSessionTs: string; consecutiveEmpty: number; currentCooldownMs: number; manualGoalDone: string }
  export function loadPerpetualState(dataDir: string, defaultCooldownMs: number): PerpetualState  // 檔損壞/缺失→預設值（fail-open）
  export function savePerpetualState(dataDir: string, s: PerpetualState): void                    // tmp+rename 原子寫
  ```
- **authorGoal 內部規格（spec §3.2-4，安全硬線）**：
  1. LLM prompt 要求輸出兩段：`OBJECTIVE:` 開頭的目標描述（含完成定義；lens=correctness 時 prompt 額外強制加入「第一步先寫 failing test 重現，無法重現回報 NOT-REPRODUCIBLE」段——M10.2 §5.1 的鉤子先埋，M10.0 不啟用判讀）與 `EVIDENCE:` 每行一個佐證檔相對路徑（0~4 個）。
  2. 佐證路徑逐一 `existsSync(resolve(cfg.projectPath, p))` 驗證＋路徑圍欄（resolve 後必須位於 projectPath 內，鏡像 evaluator.ts gatherEvidence 的 prefix fence），不合格剔除。
  3. **GOAL.md 由程式模板組裝，不是 LLM 全文生成**：
     ```
     <!-- adng:auto-goal problem:<fingerprint> -->
     # GOAL

     <OBJECTIVE 文字>

     ## 驗收

     ```sh
     <cfg.verifyCommand 逐字>
     ```

     連續無進展上限：2

     ## 佐證檔案
     <過濾後清單，每行 - 前綴；空清單則整段省略>
     ```
  4. 組裝後 `parseGoal` 回讀自驗：objective 非空、verifyCommand === cfg.verifyCommand，不符回 null。
  5. cfg.verifyCommand 未設 → 直接回 null（無機械驗收不立案）。

- [ ] **Step 1: 寫失敗測試** `tests/autopilot-author.test.ts`（LLM 以注入假函式測——author.ts 的 llm 參數收 chat 函式或設計為可注入，實作時以最小改動選擇；下列測試以「注入假 chat」為準）

```ts
import { describe, test, expect } from 'vitest'
import { mkdtempSync, writeFileSync, mkdirSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { authorGoal, isAutoGoal, loadPerpetualState, savePerpetualState } from '../src/autopilot/author.js'
import { parseGoal } from '../src/autopilot/goal.js'

const problem = { value: 8, title: 'X 模組缺錯誤處理測試', lens: 'tests', rationale: 'coverage 缺口' }

function cfgWith(projectPath: string) {
  return { projectPath, verifyCommand: 'npm test' } as never // 只用到這兩欄；型別以實作簽名為準
}

describe('authorGoal', () => {
  test('正常成案：模板組裝＋parseGoal 可回讀＋fence 逐字＝cfg.verifyCommand＋標記行', async () => {
    const dir = mkdtempSync(join(tmpdir(), 'adng-author-'))
    writeFileSync(join(dir, 'a.py'), 'x')
    const fakeChat = async () => 'OBJECTIVE: 補齊 X 模組錯誤處理測試，全部斷言具體行為。\nEVIDENCE:\na.py\nmissing.py'
    const md = await authorGoal(fakeChat as never, problem, cfgWith(dir), 'fp1234'.padEnd(16, '0'))
    expect(md).not.toBeNull()
    expect(isAutoGoal(md!)).toBe(true)
    const g = parseGoal(md!)
    expect(g.objective).toContain('X 模組')
    expect(g.verifyCommand).toBe('npm test')
    expect(g.noProgressLimit).toBe(2)
    expect(g.evidenceFiles).toEqual(['a.py'])   // missing.py 被 existsSync 剔除
  })

  test('無 verifyCommand → null（無機械驗收不立案）', async () => {
    const dir = mkdtempSync(join(tmpdir(), 'adng-author-'))
    const md = await authorGoal((async () => 'OBJECTIVE: x') as never, problem, { projectPath: dir } as never, 'fp')
    expect(md).toBeNull()
  })

  test('LLM 輸出無 OBJECTIVE → null', async () => {
    const dir = mkdtempSync(join(tmpdir(), 'adng-author-'))
    const md = await authorGoal((async () => '亂七八糟') as never, problem, cfgWith(dir), 'fp')
    expect(md).toBeNull()
  })

  test('佐證路徑圍欄：../ 逃逸被剔除', async () => {
    const dir = mkdtempSync(join(tmpdir(), 'adng-author-'))
    mkdirSync(join(dir, 'sub')); writeFileSync(join(dir, 'sub', 'ok.py'), 'x')
    const fakeChat = async () => 'OBJECTIVE: 目標。\nEVIDENCE:\nsub/ok.py\n../../etc/passwd'
    const md = await authorGoal(fakeChat as never, problem, cfgWith(dir), 'fp')
    expect(parseGoal(md!).evidenceFiles).toEqual(['sub/ok.py'])
  })
})

describe('PerpetualState', () => {
  test('round-trip＋損壞容錯', () => {
    const dir = mkdtempSync(join(tmpdir(), 'adng-state-'))
    expect(loadPerpetualState(dir, 6000).currentCooldownMs).toBe(6000)  // 缺檔→預設
    savePerpetualState(dir, { lastSessionTs: 't1', consecutiveEmpty: 2, currentCooldownMs: 12000, manualGoalDone: 'ab' })
    expect(loadPerpetualState(dir, 6000)).toEqual({ lastSessionTs: 't1', consecutiveEmpty: 2, currentCooldownMs: 12000, manualGoalDone: 'ab' })
    writeFileSync(join(dir, 'perpetual-state.json'), '{{{')
    expect(loadPerpetualState(dir, 6000).currentCooldownMs).toBe(6000)  // 損壞→預設（fail-open）
  })
})
```

- [ ] **Step 2: RED**。
- [ ] **Step 3: 實作 author.ts** — 按 Interfaces 規格；llm 參數設計為直接收 `chat` 型函式（`(prompt: string) => Promise<string>`），生產呼叫端（Task 4）負責用 llm.ts 綁定 url/model/key——author.ts 因此零 LLM 依賴、純邏輯可測。
- [ ] **Step 4: GREEN＋build＋kernel 帳**（本 task 應為 0 kernel 增量）。
- [ ] **Step 5: Commit** — `feat(autopilot): 自寫 GOAL 模板組裝(verify 逐字硬線+佐證圍欄)+外環狀態檔`。

---

### Task 4: 外環主邏輯 perpetual.ts

**Files:**
- Create: `src/autopilot/perpetual.ts`
- Test: `tests/autopilot-perpetual.test.ts`

**Interfaces:**
- Consumes: Task 1 `ProblemsLedger`/`problemFingerprint`、Task 2 `runGoalWithDeps`/`SessionResult`、Task 3 `authorGoal`/`isAutoGoal`/`loadPerpetualState`/`savePerpetualState`、既有 `discoverProblems`（discover.js）、`parseGoal`（goal.js）、`Deps`（scheduler.js）。
- Produces（Task 5 daemon 接線依賴）:
  ```ts
  export interface PerpetualHooks {   // 測試注入面；生產由 maybeRunPerpetual 綁定真實實作
    now(): Date
    discover(): Promise<DiscoverResult | undefined>
    author(problem: RankedProblem, fingerprint: string): Promise<string | null>
    runSession(opts: { discovered?: DiscoverResult }): Promise<SessionResult | 'no-goal' | 'lock-busy'>
    billedToday(): number
  }
  export async function runPerpetualCycle(cfg: Config, dataDir: string, events: EventLog, notify: (t: string) => Promise<boolean>, hooks: PerpetualHooks): Promise<boolean>
  export async function maybeRunPerpetual(deps: Deps, notifier: { send(t: string): Promise<boolean> }): Promise<boolean>  // 生產薄殼：綁定真 hooks 後呼叫 runPerpetualCycle
  export function perpetualDigestLine(dataDir: string): string | null   // 台帳 counts 摘要；任何故障回 null（fail-open）
  ```
- **runPerpetualCycle 流程（spec §3.1/§3.2/§3.5 全文為準）**：
  1. 前置閘依序：`cfg.perpetual !== true` → false；`existsSync(cfg.stopFile)` → false；`hooks.billedToday() >= cfg.dailyHardUsd` → false；冷卻窗（`now - state.lastSessionTs < state.currentCooldownMs`）→ false。任一擋下皆不寫狀態、不發事件（安靜讓路）。
  2. `cfg.goalFile` 存在：讀檔——無 auto-goal 標記＝手動 GOAL：goalId=sha1(objective).slice(0,4)（與 run.ts 慣例一致），若 === state.manualGoalDone → false（防重跑）；否則 `runSession({})`，成功後 state.manualGoalDone=goalId、lastSessionTs 更新，發通知，回 true。有 auto-goal 標記＝前次 crash 殘留：直接 `runSession({})` 續跑，收案照 6.（fingerprint 從標記行解析）。
  3. 無 GOAL：`hooks.discover()`；undefined 或 ranked 空 → `perpetual-no-case` 事件、state.consecutiveEmpty++、lastSessionTs 更新、回 false（M10.0 冷卻不翻倍，M10.2 再加）。
  4. ranked 逐條 `ledger.upsertSeen`（now ISO）；候選＝status open 且 value ≥ `cfg.perpetualValueThreshold`，按 value DESC 取前 3 逐一嘗試 `hooks.author()`；全 null → 每個記 `setStatus(fp,'deferred','goal-authoring-failed')`、`perpetual-no-case` 事件、回 false。
  5. 成案：GOAL.md 先寫 tmp 再 rename 至 cfg.goalFile；ledger `setStatus(fp,'in-progress','',goalId)`（goalId 由 parseGoal(md).objective 雜湊）；`perpetual-goal-authored` 事件。
  6. `runSession({ discovered })`（傳入既有 discovery 結果不重跑）；收案映射：outcome.kind==='achieved' → `setStatus(fp,'fixed', note含rounds/殘餘gaps數, goalId)`；其他 kind → `setStatus(fp,'deferred', outcome.kind＋reason)`。刪除 GOAL.md（**僅限 isAutoGoal 為真時**）。state：lastSessionTs 更新、consecutiveEmpty=0。`perpetual-session-done` 事件＋Discord 通知（`自主工程師：<title> → <fixed|deferred>（<rounds> 輪）`）。回 true。
  7. 整體包 try/catch：throw → `perpetual-error` 事件（quiet 包裹）、回 false。ledger 開在 cycle 內、finally close。
- **maybeRunPerpetual 生產綁定**：discover→`discoverProblems`（run.ts/session.ts 既有建構方式照抄：finderLlm/criticLlm/runSurvey/lenses）；author→llm.ts chat 綁 judgeModel；runSession→`runGoalWithDeps(deps, notifier, cfg, opts)`；billedToday→`deps.db.billedCostForLocalDay(localDay(now, offset), offset, subscriptionTags(deps.cfg))`；notify→notifier.send。

- [ ] **Step 1: 寫失敗測試**（全用注入 hooks＋暫存目錄；每案斷言「回傳值＋台帳/狀態檔/GOAL 檔副作用」）

```ts
// tests/autopilot-perpetual.test.ts —— 骨架（完整案例依下列矩陣逐一展開）：
// 前置閘矩陣：perpetual:false／stop 檔在／billed>=hard／冷卻中 → 各自回 false 且 discover 未被呼叫（spy 計數 0）
// 手動 GOAL：無標記檔在 → runSession 被呼叫、manualGoalDone 記錄；同 goalId 第二次 → false 不重跑
// 手動 GOAL 不代刪：session 後 GOAL.md 仍存在
// auto-goal 殘留：標記檔在 → 直接 runSession 續跑
// no-case：discover 回 ranked:[] → consecutiveEmpty+1、回 false
// value 閘：全部 value<門檻 → 不 author、不 runSession
// 成案全流程：ranked 1 條 value 8 → author 回合法 GOAL → 寫檔 → runSession(achieved) → 台帳 fixed → GOAL 檔已刪 → 回 true
// deferred 映射：runSession 回 no-progress → 台帳 deferred
// author 全敗：3 候選全 null → 各記 deferred(goal-authoring-failed)
// fail-open：hooks.discover throw → perpetual-error 事件、回 false、不 throw
```

（實作者注意：`events` 用真 EventLog 對暫存 dataDir——事件檔可讀回斷言 type；`cfg` 以 `ConfigSchema.parse` 產生合法物件再覆寫欄位，勿手捏繞過 schema。）

- [ ] **Step 2: RED**。
- [ ] **Step 3: 實作 perpetual.ts**（流程如 Interfaces 節；`perpetualDigestLine`＝開唯讀 ledger 取 counts 組字串 `自主工程師台帳：open X｜fixed Y｜deferred Z`，任何 throw 回 null）。
- [ ] **Step 4: GREEN＋全套回歸＋kernel 帳**（0 kernel 增量）。
- [ ] **Step 5: Commit** — `feat(autopilot): 外環主邏輯——五重前置閘+自主立案+收案回寫台帳`。

---

### Task 5: daemon 接線＋config schema（kernel 面）

**Files:**
- Modify: `src/types.ts`（ConfigSchema 三欄）
- Modify: `src/daemon.ts`（idle 分支）
- Test: `tests/daemon.test.ts`（既有檔加案例）＋`tests/config.test.ts`（若有 schema 測試檔，加預設值斷言；無則併入 daemon 測試檔）

**Interfaces:**
- Consumes: Task 4 `maybeRunPerpetual`。
- Produces: ConfigSchema 新欄（M10.0 範圍）：
  ```ts
  // M10.0 永續外環：perpetual 未設（false）＝外環完全停用，daemon 行為與 M9.9 一致（硬回歸線）。
  perpetual: z.boolean().default(false),
  perpetualCooldownMs: z.number().int().positive().default(6 * 60 * 60 * 1000),
  perpetualValueThreshold: z.number().int().min(0).max(10).default(6),
  ```

- [ ] **Step 1: 寫失敗測試**（daemon 測試檔仿既有 runDaemon 測試模式——注入 sleepFn/maxCycles/假 deps）

```ts
// 案例 1：perpetual 未設（false）→ idle 輪行為與現狀一致（idle alert 照發、maybeRunPerpetual 未被呼叫）
//   ——用 vi.mock('./autopilot/perpetual.js') 或注入點 spy（依 daemon 測試檔既有 mock 慣例，先讀檔對齊）
// 案例 2：perpetual:true 且 maybeRunPerpetual 回 true → 該輪不發 idle alert、sleep 收到 cooldownMs（非 idleSleepMs）
// 案例 3：maybeRunPerpetual throw → daemon 不死、照發 idle alert、迴圈繼續（下一輪照跑）
```

- [ ] **Step 2: RED**。
- [ ] **Step 3: 實作** — types.ts 三欄；daemon.ts idle 分支改為（spec §3.6 逐字，含 sleep 語意與 continue）：

```ts
if (result === 'idle' || result === 'cost-hard-stop') {
  if (result === 'idle') {
    let acted = false
    try { acted = await maybeRunPerpetual(deps, notifier) } catch { /* fail-open：外環故障不反殺 daemon（鐵律 #4） */ }
    if (acted) { await sleep(cooldownMs); continue }   // session 已耗時，短冷卻即回輪
    await alert('idle', 'daemon 提醒:backlog 已耗盡,請補任務')
  }
  await sleep(idleSleepMs)
} else {
  await sleep(cooldownMs)
}
```

（既有 `if (result === 'idle') { await alert(...) }` 段（daemon.ts:297-299）併入上式刪除；import 加 `import { maybeRunPerpetual } from './autopilot/perpetual.js'`。）

- [ ] **Step 4: GREEN＋全套＋kernel 帳** — `npx vitest run; echo EXIT=$?` 全綠；`wc -l src/*.ts | tail -1` ≤2700（預估 daemon +9、types +4 → ~2562）。
- [ ] **Step 5: Commit** — `feat(daemon): idle 分支接外環——perpetual opt-in+fail-open+sleep 語意`。

---

### Task 6: 可觀測性（digest 段＋bot /problems）＋首發 config＋煙測

**Files:**
- Modify: `src/digest.ts`（自主工程師段）
- Modify: `src/daemon.ts`（checkAndSendDigest 傳 perpetualDigestLine——僅呼叫點 +2 行）
- Modify: `src/bot/handlers.ts`（/problems 指令；bot/ 子目錄不吃 kernel 帳）
- Modify: `src/bot/index.ts`（指令註冊——先讀檔對齊既有指令註冊模式）
- Modify: `configs/prompt-autoresearch.json`（`"perpetual": true`）
- Test: `tests/digest.test.ts`＋`tests/handlers.test.ts`（既有檔加案例）

**Interfaces:**
- Consumes: Task 4 `perpetualDigestLine`、Task 1 `ProblemsLedger`。
- Produces: `buildDigest` 入參物件加可選欄 `perpetualLine?: string | null`（undefined/null＝整段省略，既有呼叫端不改也編譯——向後相容）；digest 文末加一行（有值時）。`cmdProblems(deps): CmdResult`（列 open top10：`v<value> [<lens>] <title>`）。

- [ ] **Step 1: 寫失敗測試** — digest：`perpetualLine` 有值出現在輸出、null/undefined 完全不出現「自主工程師」字樣；handlers：/problems 對暫存 run.db 預灌 3 筆（2 open 1 fixed）回 open 兩筆按 value DESC。
- [ ] **Step 2: RED**。
- [ ] **Step 3: 實作**（digest +~5 行 kernel；daemon 呼叫點 `perpetualLine: quietValue(() => perpetualDigestLine(dataDir))` 風格，fail-open null）。
- [ ] **Step 4: GREEN＋全套＋kernel 帳 ≤2700。**
- [ ] **Step 5: 煙測** — `node dist/cli.js status --config configs/prompt-autoresearch.json` 正常；`node dist/cli.js run-once --config configs/prompt-autoresearch.json` 在 backlog 空＋無 GOAL 下回 idle（run-once 不觸外環——外環只掛 daemon）。
- [ ] **Step 6: Commit** — `feat(observability): digest 自主工程師段+bot /problems+prompt-autoresearch 開 perpetual`（config 檔同 commit）。

---

## 收尾

- [ ] 全套綠（611+新增）＋kernel ≤2700 實測貼數字。
- [ ] fresh reviewer 全分支審，重點清單：①循環 import 真的斷了（`rg "from '\.\./cli" src/autopilot/` 零命中、`rg "from './autopilot" src/daemon.ts` 僅 perpetual）②perpetual:false 回歸線（daemon 測試案例 1）③自寫 GOAL 的 verify 逐字硬線與佐證圍欄④台帳 fail-open（擋工作 vs 失憶的方向）⑤手動 GOAL 不代刪＋防重跑。
- [ ] 修 review 發現 → merge --no-ff → push → 產線重啟（daemon）→ ledger 記錄。
- [ ] 真線驗收（spec §9）：Prompt AutoResearch 清空 backlog＋無 GOAL，觀察 daemon 冷卻窗後自主立案一輪，台帳/digest/Discord 三處留痕。
