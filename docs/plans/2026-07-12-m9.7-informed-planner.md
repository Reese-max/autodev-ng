# M9.7 Informed Problem-Finding Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓 autopilot 的 planner 從「瞎拆給定目標」升級到「掃專案→多鏡頭發現→對抗式排序→攻最高價值問題」，且發現側也被獨立驗證。

**Architecture:** 新模組 `src/autopilot/discover.ts`：surveyCommand 機械接地 → 五鏡頭並行 finder → 對抗式 critic 排序。`run.ts` 每 session 開頭跑一次 `discoverProblems`，把排序問題清單注入 orchestrator 的 `repoSummary`，planner 據此攻最高價值問題。全在 `src/autopilot/`（不計 kernel 帳），opt-in（`surveyCommand` 未設＝維持現狀）。

**Tech Stack:** TypeScript（node ≥22）、vitest（serial：`maxWorkers 1`＋`testTimeout 20s`）、zod（config）、`callAgent`（`src/autopilot/llm.ts`，回 `{text, totalTokens}`）。

## Global Constraints

- **kernel 行數帳**：`wc -l src/*.ts`（頂層 16 檔）**上限 2700**（工作上限，硬牆 3000）。本功能改動主要在 `src/autopilot/`（不計帳）；只有 `src/types.ts`（config 新欄位）計帳，現值 2503，加 3 欄約 +3 → ~2506 ≤2700。每動 `src/*.ts` 頂層檔的 task 結束實測 `wc -l src/*.ts | tail -1`。
- **測試**：`npm run build`（tsc）＋`npx vitest run`（serial）全綠。禁改 `vitest.config.ts`。測試禁 sleep 賭時序。
- **fail-open 為最高原則**：discovery 是加值，任一環節（survey/finder/critic）故障都不得 throw 到 autopilot——退回原始候選或空清單或 `"round N"`，絕不擋主流程。
- **LlmOpts 形狀**：`{ url?: string; model: string; apiKey: string; fetchFn?: typeof fetch }`。`callAgent(opts, prompt)` 回 `{ text, totalTokens }`；`opts.url` 缺省時 fail-open 回 `{text:'', totalTokens:0}`。
- **測試注入慣例**：LLM mock 用 `fetchFn`（見既有 `tests/autopilot-evaluator.test.ts`）：`fetchFn: (async () => ({ ok:true, status:200, json: async () => ({ choices:[{ message:{ content } }] }) })) as unknown as typeof fetch`。檔案讀取用可注入的 `readEvidence`/`runSurvey`（勿真碰 fs）。
- **複用 M9.5 的 `gatherEvidence`**：`import { gatherEvidence } from './evaluator.js'`（已 export；每檔 8000 字元、總 24000 字元、路徑圍欄 fail-open）。
- **conventional commit** 繁中，結尾：
  ```
  Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01S5pF57FfHKaCaHPTXEnMso
  ```
- commit 前 `git diff --cached --name-only` 嚴格核對；分支 `m9.7-informed-planner`；不 push（收尾統一 merge）。

---

### Task 1: 解析函式（parseCandidates + parseRanked）

**Files:**
- Create: `src/autopilot/discover.ts`（僅本 task 的型別與兩個純解析函式）
- Test: `tests/autopilot-discover.test.ts`

**Interfaces:**
- Produces:
  - `export interface Candidate { lens: string; title: string; detail: string }`
  - `export interface RankedProblem { title: string; lens: string; value: number; rationale: string }`
  - `export function parseCandidates(lens: string, out: string): Candidate[]` —— finder 回應每行 `<標題>｜<理由>`（全形或半形分隔 `｜`/`|`），回候選；首個非空行為 `NONE`（不分大小寫）→ 空陣列；無分隔符的行以整行為 title、detail 空。
  - `export function parseRanked(out: string): RankedProblem[]` —— 每行 `VALUE:<0~10> | <title> | <lens> | <rationale>`；value 夾 0~10；不符格式的行跳過；全空 → 空陣列。

- [ ] **Step 1: 寫失敗測試**

```ts
import { describe, test, expect } from 'vitest'
import { parseCandidates, parseRanked } from '../src/autopilot/discover.js'

describe('parseCandidates', () => {
  test('多行候選（全形｜分隔）', () => {
    const r = parseCandidates('tests', 'lib/api.py 無 call_args 斷言｜只驗呼叫不驗參數\nlib/io.py 缺錯誤路徑測試｜read_jsonl 檔案不存在分支未測')
    expect(r).toEqual([
      { lens: 'tests', title: 'lib/api.py 無 call_args 斷言', detail: '只驗呼叫不驗參數' },
      { lens: 'tests', title: 'lib/io.py 缺錯誤路徑測試', detail: 'read_jsonl 檔案不存在分支未測' }
    ])
  })
  test('半形 | 分隔也可', () => {
    expect(parseCandidates('perf', 'X 全掃無快取|每次 O(n)')[0]).toEqual({ lens: 'perf', title: 'X 全掃無快取', detail: '每次 O(n)' })
  })
  test('NONE → 空陣列', () => { expect(parseCandidates('security', 'NONE')).toEqual([]) })
  test('無分隔符行 → 整行為 title', () => { expect(parseCandidates('design', '模組職責糾纏')).toEqual([{ lens: 'design', title: '模組職責糾纏', detail: '' }]) })
})

describe('parseRanked', () => {
  test('正常排序行', () => {
    const r = parseRanked('VALUE:9 | api.py 斷言弱 | tests | 高頻對外呼叫沒驗參數\nVALUE:4 | 命名不一致 | design | 影響小')
    expect(r).toEqual([
      { value: 9, title: 'api.py 斷言弱', lens: 'tests', rationale: '高頻對外呼叫沒驗參數' },
      { value: 4, title: '命名不一致', lens: 'design', rationale: '影響小' }
    ])
  })
  test('value 超界夾 0~10', () => { expect(parseRanked('VALUE:15 | X | perf | y')[0]!.value).toBe(10) })
  test('不符格式行跳過、全空 → 空陣列', () => {
    expect(parseRanked('我覺得都還好\n沒有明確問題')).toEqual([])
  })
})
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npx vitest run tests/autopilot-discover.test.ts`
Expected: FAIL（`parseCandidates`/`parseRanked` 未定義或 discover.ts 不存在）。

- [ ] **Step 3: 實作**

`src/autopilot/discover.ts`：

```ts
export interface Candidate { lens: string; title: string; detail: string }
export interface RankedProblem { title: string; lens: string; value: number; rationale: string }

// finder 回應：每行 <標題>｜<理由>（全形｜或半形|）；首個非空行 NONE → 空；無分隔→整行 title。
export function parseCandidates(lens: string, out: string): Candidate[] {
  const lines = out.split(/\r?\n/).map(l => l.trim()).filter(Boolean)
  if (lines.length === 0 || /^NONE\b/i.test(lines[0]!)) return []
  return lines.map(l => {
    const idx = l.search(/[｜|]/)
    if (idx < 0) return { lens, title: l, detail: '' }
    return { lens, title: l.slice(0, idx).trim(), detail: l.slice(idx + 1).trim() }
  }).filter(c => c.title)
}

// critic 回應：每行 VALUE:<n> | title | lens | rationale；夾 0~10；不符格式跳過。
export function parseRanked(out: string): RankedProblem[] {
  const ranked: RankedProblem[] = []
  for (const raw of out.split(/\r?\n/)) {
    const m = raw.match(/VALUE\s*[:：]\s*(\d+)\s*[|｜]\s*([^|｜]+?)\s*[|｜]\s*([^|｜]+?)\s*[|｜]\s*(.+)/i)
    if (!m) continue
    ranked.push({ value: Math.min(10, Math.max(0, Number(m[1]))), title: m[2]!.trim(), lens: m[3]!.trim(), rationale: m[4]!.trim() })
  }
  return ranked
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `npx vitest run tests/autopilot-discover.test.ts`
Expected: PASS（7 tests）。

- [ ] **Step 5: Commit**

```bash
git add src/autopilot/discover.ts tests/autopilot-discover.test.ts
git diff --cached --name-only   # 核對 = 這 2 檔
git commit -m "feat(autopilot): discover.ts parseCandidates/parseRanked 解析函式"
```

---

### Task 2: discoverProblems 串接（survey→多鏡頭 finder→critic）

**Files:**
- Modify: `src/autopilot/discover.ts`（加 `DiscoverDeps`/`DiscoverResult`/`discoverProblems` 及內部 prompt helper）
- Test: `tests/autopilot-discover.test.ts`（加 `discoverProblems` 案例）

**Interfaces:**
- Consumes: Task 1 的 `Candidate`/`RankedProblem`/`parseCandidates`/`parseRanked`；`callAgent`（`./llm.js`）；`gatherEvidence`（`./evaluator.js`）；`Goal`（`./goal.js`）；`LlmOpts`（`./llm.js`）。
- Produces:
  - `export interface DiscoverDeps { finderLlm: LlmOpts; criticLlm: LlmOpts; runSurvey?: (cmd: string, cwd: string) => { output: string }; readEvidence?: (absPath: string) => string; lenses: string[] }`
  - `export interface DiscoverResult { survey: string; ranked: RankedProblem[] }`
  - `export async function discoverProblems(deps: DiscoverDeps, goal: Goal, cwd: string): Promise<DiscoverResult>` —— 流程：跑 survey（`deps.runSurvey?.('', cwd)`；**survey 指令綁在 deps.runSurvey 閉包內**，由呼叫端 run.ts 提供，本函式不需知道指令字串，故第一參數傳空字串）→ 五鏡頭並行 `callAgent(finderLlm, finderPrompt(lens, survey, evidence))` → `parseCandidates` 收候選 → `callAgent(criticLlm, criticPrompt(candidates))` → `parseRanked`。全程 fail-open（見 Step 3）。

- [ ] **Step 1: 寫失敗測試**

```ts
import { discoverProblems } from '../src/autopilot/discover.js'
import type { Goal } from '../src/autopilot/goal.js'

const goal: Goal = { objective: '持續改善品質', noProgressLimit: 3, evidenceFiles: ['a.py'] }
// 依「呼叫序」回應：前 N 次是 finder（每鏡頭一次），最後一次是 critic
const seqLlm = (responses: string[]) => {
  let i = 0
  return { url: 'http://x/v1', model: 'm', apiKey: 'k',
    fetchFn: (async () => ({ ok: true, status: 200,
      json: async () => ({ choices: [{ message: { content: responses[Math.min(i++, responses.length - 1)] } }] }) })) as unknown as typeof fetch }
}

describe('discoverProblems', () => {
  test('survey→finder→critic 串接，回排序問題', async () => {
    const finder = seqLlm(['問題A｜理由a', 'NONE', 'NONE', 'NONE', 'NONE'])
    const critic = seqLlm(['VALUE:8 | 問題A | correctness | 高價值'])
    const r = await discoverProblems({ finderLlm: finder, criticLlm: critic,
      runSurvey: () => ({ output: 'coverage 40%' }), readEvidence: () => 'code', lenses: ['correctness', 'tests', 'perf', 'design', 'security'] }, goal, '/proj')
    expect(r.survey).toContain('coverage 40%')
    expect(r.ranked).toEqual([{ value: 8, title: '問題A', lens: 'correctness', rationale: '高價值' }])
  })
  test('critic 亂格式 → fail-open 空 ranked，不崩', async () => {
    const finder = seqLlm(['問題X｜y'])
    const critic = seqLlm(['我覺得都還好'])
    const r = await discoverProblems({ finderLlm: finder, criticLlm: critic,
      runSurvey: () => ({ output: 's' }), readEvidence: () => 'c', lenses: ['correctness'] }, goal, '/proj')
    expect(r.ranked).toEqual([])
  })
  test('survey runSurvey throw → survey 空，仍能跑 finder/critic（fail-open）', async () => {
    const r = await discoverProblems({ finderLlm: seqLlm(['NONE']), criticLlm: seqLlm(['']),
      runSurvey: () => { throw new Error('survey boom') }, readEvidence: () => 'c', lenses: ['correctness'] }, goal, '/proj')
    expect(r.survey).toBe(''); expect(r.ranked).toEqual([])
  })
  test('finder throw 該鏡頭跳過，其餘鏡頭仍出候選', async () => {
    let n = 0
    const finder = { url: 'http://x/v1', model: 'm', apiKey: 'k',
      fetchFn: (async () => { n++; if (n === 1) throw new Error('finder boom'); return { ok: true, status: 200,
        json: async () => ({ choices: [{ message: { content: '問題B｜b' } }] }) } }) as unknown as typeof fetch }
    const critic = seqLlm(['VALUE:5 | 問題B | tests | ok'])
    const r = await discoverProblems({ finderLlm: finder, criticLlm: critic,
      runSurvey: () => ({ output: 's' }), readEvidence: () => 'c', lenses: ['correctness', 'tests'] }, goal, '/proj')
    expect(r.ranked[0]!.title).toBe('問題B')
  })
})
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npx vitest run tests/autopilot-discover.test.ts`
Expected: FAIL（`discoverProblems` 未定義）。

- [ ] **Step 3: 實作**

在 `discover.ts` 加（`runSurvey` 已由呼叫端綁定 surveyCommand，本函式以空字串呼叫，指令綁在 deps 閉包內；prompt helper 為模組私有）：

```ts
import type { Goal } from './goal.js'
import { callAgent, type LlmOpts } from './llm.js'
import { gatherEvidence } from './evaluator.js'

export interface DiscoverDeps {
  finderLlm: LlmOpts
  criticLlm: LlmOpts
  runSurvey?: (cmd: string, cwd: string) => { output: string }
  readEvidence?: (absPath: string) => string
  lenses: string[]
}
export interface DiscoverResult { survey: string; ranked: RankedProblem[] }

function finderPrompt(lens: string, survey: string, evidence: string): string {
  return [
    `你是「${lens}」視角的問題發現者。只從「${lens}」角度，找出專案的具體問題（0~5 條，沒有回 NONE）。`,
    '每行一問題：<簡短標題>｜<一句證據/理由>',
    `\n# 勘查訊號\n${survey || '（無）'}`,
    `\n# 佐證檔案\n${evidence || '（無）'}`
  ].join('\n')
}
function criticPrompt(cands: Candidate[]): string {
  const body = cands.map(c => `[${c.lens}] ${c.title}｜${c.detail}`).join('\n')
  return [
    '你是對抗式問題評審。以下是多視角候選問題。去重、挑戰每個（真問題嗎？夠高價值嗎？漏了更重要的嗎？），按修復價值排序。',
    '嚴格照格式，每行一問題（高價值在前）：VALUE:<0~10> | <標題> | <lens> | <一句理由>',
    `\n候選：\n${body || '（無）'}`
  ].join('\n')
}

export async function discoverProblems(deps: DiscoverDeps, goal: Goal, cwd: string): Promise<DiscoverResult> {
  let survey = ''
  if (deps.runSurvey) { try { survey = deps.runSurvey('', cwd).output.slice(0, 8000) } catch { survey = '' } }
  const evidence = gatherEvidence(goal.evidenceFiles, cwd, deps.readEvidence)
  const found = await Promise.all(deps.lenses.map(async (lens) => {
    try { return parseCandidates(lens, (await callAgent(deps.finderLlm, finderPrompt(lens, survey, evidence))).text.trim()) }
    catch { return [] as Candidate[] }
  }))
  const candidates = found.flat()
  let ranked: RankedProblem[] = []
  try { ranked = parseRanked((await callAgent(deps.criticLlm, criticPrompt(candidates))).text.trim()) }
  catch { ranked = [] }
  return { survey, ranked }
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `npx vitest run tests/autopilot-discover.test.ts`
Expected: PASS（Task 1 的 7 + 本 task 4 = 11 tests）。

- [ ] **Step 5: 驗證＋Commit**

```bash
npm run build   # exit 0
git add src/autopilot/discover.ts tests/autopilot-discover.test.ts
git diff --cached --name-only   # 核對 = 這 2 檔
git commit -m "feat(autopilot): discoverProblems 串接——survey+多鏡頭 finder+對抗式 critic"
```

---

### Task 3: config 新欄位（surveyCommand / surveyTimeoutMs / discoverLenses）

**Files:**
- Modify: `src/types.ts`（ConfigSchema 加 3 欄，near judgeApiKey/auditModel）
- Test: `tests/autopilot-goal.test.ts`（config describe 加案例）

**Interfaces:**
- Consumes: 既有 `ConfigSchema`。
- Produces: `cfg.surveyCommand?: string`；`cfg.surveyTimeoutMs: number`（預設 120000）；`cfg.discoverLenses: string[]`（預設 `['correctness','tests','perf','design','security']`）。

- [ ] **Step 1: 寫失敗測試**

`tests/autopilot-goal.test.ts` 的 `describe('config goalFile', ...)` 內加：

```ts
  test('M9.7：surveyCommand 可選、surveyTimeoutMs/discoverLenses 有預設（向後相容）', () => {
    const bare = ConfigSchema.parse({ projectPath: '/p', backlogFile: '/p/B.md', dataDir: '/p/d', engine: 'mock' })
    expect(bare.surveyCommand).toBeUndefined()
    expect(bare.surveyTimeoutMs).toBe(120000)
    expect(bare.discoverLenses).toEqual(['correctness', 'tests', 'perf', 'design', 'security'])
    const set = ConfigSchema.parse({ projectPath: '/p', backlogFile: '/p/B.md', dataDir: '/p/d', engine: 'mock', surveyCommand: 'ruff check .', discoverLenses: ['tests'] })
    expect(set.surveyCommand).toBe('ruff check .'); expect(set.discoverLenses).toEqual(['tests'])
  })
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npx vitest run tests/autopilot-goal.test.ts`
Expected: FAIL（`surveyCommand`/`surveyTimeoutMs`/`discoverLenses` 不在 schema）。

- [ ] **Step 3: 實作**

`src/types.ts` ConfigSchema，在 `auditModel`/`supplementLimit` 那組後面加：

```ts
  // M9.7 informed problem-finding：surveyCommand 未設＝discovery 不啟動（planner 維持 "round N"）。
  surveyCommand: z.string().optional(),
  surveyTimeoutMs: z.number().int().positive().default(120000),
  discoverLenses: z.array(z.string()).default(['correctness', 'tests', 'perf', 'design', 'security']),
```

- [ ] **Step 4: 跑測試＋kernel 帳**

```bash
npm run build   # exit 0
npx vitest run tests/autopilot-goal.test.ts   # PASS
wc -l src/*.ts | tail -1   # 記錄值，須 ≤2700
```
Expected: 測試 PASS；kernel ~2506 ≤2700。

- [ ] **Step 5: Commit**

```bash
git add src/types.ts tests/autopilot-goal.test.ts
git diff --cached --name-only   # 核對 = 這 2 檔
git commit -m "feat(config): M9.7 surveyCommand/surveyTimeoutMs/discoverLenses(discovery opt-in)"
```

---

### Task 4: 整合——run.ts 跑 discovery + orchestrator 注入 repoSummary + planner prompt

**Files:**
- Modify: `src/autopilot/orchestrator.ts`（`OrchestratorDeps` 加可選 `discovered?: { survey: string; ranked: RankedProblem[] }`；`repoSummary` 建構改用它）
- Modify: `src/autopilot/run.ts`（`surveyCommand` 有設時 session 開頭跑 `discoverProblems`，結果傳入 `orchDeps.discovered`；fail-open）
- Modify: `src/autopilot/planner.ts`（`buildPrompt` 提示微調，引導從排序問題挑最高價值未處理者）
- Test: `tests/autopilot-orchestrator.test.ts`（repoSummary 注入案例）

**Interfaces:**
- Consumes: Task 2 的 `discoverProblems`/`DiscoverResult`/`RankedProblem`；既有 `OrchestratorDeps`/`runGoalSession`（`./orchestrator.js`）；`assemble`（`../cli.js`）。
- Produces: orchestrator 每輪 `repoSummary`＝有 `discovered` 時為「勘查＋排序問題清單」，否則 `"round N"`（向後相容）。

- [ ] **Step 1: 寫失敗測試**

`tests/autopilot-orchestrator.test.ts` 加（複用該檔既有 `runGoalSession` 測試風格，注入 `planFn` 捕捉 `repoSummary`）：

```ts
  test('M9.7：discovered 有排序問題時 repoSummary 含問題清單', async () => {
    let seenSummary = ''
    const deps = {
      goalId: 'g', goal: { objective: 'o', noProgressLimit: 1 }, cwd: '/p', kernelDeps: {} as never, lessonsText: '',
      discovered: { survey: 'coverage 40%', ranked: [{ value: 9, title: 'api 斷言弱', lens: 'tests', rationale: '高頻' }] },
      planFn: async (input: { repoSummary: string }) => { seenSummary = input.repoSummary; return { kind: 'achieved' as const } },
      evalFn: async () => ({ achieved: true, score: 1, detail: '' }),
      runOnceFn: async () => 'done' as never, isAlive: () => true
    }
    await runGoalSession(deps as never)
    expect(seenSummary).toContain('coverage 40%')
    expect(seenSummary).toContain('api 斷言弱')
  })
  test('M9.7：無 discovered → repoSummary 維持 round N（向後相容）', async () => {
    let seenSummary = ''
    const deps = {
      goalId: 'g', goal: { objective: 'o', noProgressLimit: 1 }, cwd: '/p', kernelDeps: {} as never, lessonsText: '',
      planFn: async (input: { repoSummary: string }) => { seenSummary = input.repoSummary; return { kind: 'achieved' as const } },
      evalFn: async () => ({ achieved: true, score: 1, detail: '' }),
      runOnceFn: async () => 'done' as never, isAlive: () => true
    }
    await runGoalSession(deps as never)
    expect(seenSummary).toMatch(/round 1/)
  })
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `npx vitest run tests/autopilot-orchestrator.test.ts`
Expected: FAIL（`OrchestratorDeps` 無 `discovered`，repoSummary 恆 "round N"）。

- [ ] **Step 3: 實作 orchestrator**

`src/autopilot/orchestrator.ts`：`import type { RankedProblem } from './discover.js'`；`OrchestratorDeps` 介面加 `discovered?: { survey: string; ranked: RankedProblem[] }`；把 `const repoSummary = \`round ${round}\`` 改為：

```ts
    const repoSummary = deps.discovered && deps.discovered.ranked.length
      ? [
          `round ${round}`,
          `# 專案勘查\n${deps.discovered.survey.slice(0, 2000)}`,
          '# 已排序的待解問題（高價值在前；配下方歷史挑最高價值且未處理者）',
          ...deps.discovered.ranked.map((p, i) => `${i + 1}. [value ${p.value}] ${p.title}（${p.lens}）— ${p.rationale}`)
        ].join('\n')
      : `round ${round}`
```

- [ ] **Step 4: 實作 planner 提示微調**

`src/autopilot/planner.ts` `buildPrompt`：在現有第一行指示後補一句（找到 `'你是自主開發規劃器。...'` 那行，改為兩行）：

```ts
    '你是自主開發規劃器。目標如下，判斷為達成目標「下一批」該做哪些具體任務。',
    '若 repo 現況含「已排序的待解問題」，優先挑其中最高價值且未在歷史中處理過的問題，為它拆任務。',
```

- [ ] **Step 5: 實作 run.ts 接線**

`src/autopilot/run.ts`：`import { discoverProblems } from './discover.js'`。在 `const llm = {...}` 之後、`const orchDeps = {...}` 之前加：

```ts
    // M9.7：session 開頭跑一次 discovery（僅 cfg.surveyCommand 有設；fail-open——故障退回無 discovered）。
    let discovered: { survey: string; ranked: import('./discover.js').RankedProblem[] } | undefined
    if (cfg.surveyCommand) {
      try {
        discovered = await discoverProblems({
          finderLlm: llm,
          criticLlm: { url: cfg.judgeUrl, model: cfg.auditModel ?? cfg.judgeModel, apiKey: cfg.judgeApiKey },
          runSurvey: (_c, wd) => {
            try { return { output: execSync(cfg.surveyCommand!, { cwd: wd, encoding: 'utf8', stdio: ['ignore', 'pipe', 'pipe'], timeout: cfg.surveyTimeoutMs }).slice(-8000) } }
            catch (e) { const er = e as { stdout?: string }; return { output: (er.stdout ?? '').slice(-8000) } }
          },
          lenses: cfg.discoverLenses
        }, goal, cfg.projectPath)
        appendFileSync(auditFile, JSON.stringify({ discovered }) + '\n')
      } catch (e) { console.error('discovery 故障（fail-open，無 discovered）:', String(e)) }
    }
```

並在 `orchDeps` 物件字面值加一欄 `discovered,`（`execSync` 已於 M9.6 import；若無則加 `import { execSync } from 'node:child_process'`）。

- [ ] **Step 6: 驗證＋Commit**

```bash
npm run build   # exit 0
npx vitest run   # 全套全綠
wc -l src/*.ts | tail -1   # ≤2700（本 task 只動子目錄，kernel 不增）
git add src/autopilot/orchestrator.ts src/autopilot/run.ts src/autopilot/planner.ts tests/autopilot-orchestrator.test.ts
git diff --cached --name-only   # 核對 = 這 4 檔
git commit -m "feat(autopilot): 整合 discovery——run.ts 跑一次+orchestrator 注入 repoSummary+planner 提示"
```

---

## 收尾（merge 前）

- [ ] 全套：`npm run build && npx vitest run`（全綠）＋`wc -l src/*.ts | tail -1`（≤2700，記值）。
- [ ] 驗收派 fresh reviewer（typescript-reviewer）審全分支 `git diff main..m9.7-informed-planner`：重點 fail-open 完整性（survey/finder/critic 每路徑）、獨立性（criticLlm 用 auditModel）、向後相容（無 surveyCommand→"round N"）、並行 finder 正確、kernel 帳。
- [ ] 修 Critical/Important，re-review。
- [ ] 寫 `.superpowers/sdd/m9.7-report.md`＋更新 `progress.md` ledger。
- [ ] merge 回 main（`--no-ff`）+ push。不在中途 push。
